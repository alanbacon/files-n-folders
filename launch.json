{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run Tests",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceRoot}/node_modules/.bin/jest",
      "runtimeExecutable": "${env:HOME}/.nvm/versions/node/v20.5.0/bin/node"
    }
  ]
}